# No secrets are needed yet (Phase 2). This file exists as a placeholder
# per project convention: real secrets go in .env (gitignored), never here.
# Populated starting Phase 18 (FastAPI inference server) / Phase 19 (API security).
